---
id: c_19f627ba14e_4
type: note
state: done
kind: task
anchor:
  file: apps/api/src/main/kotlin/com/cuebiq/impressions/seed/SeedRunner.kt
  scope: range
  startLine: 127
  endLine: 127
context:
  snippet: '            val name = (props.get("level1") ?: props.get("name"))?.asString()'
  before: |2-
            val byName = LinkedHashMap<String, MutableList<List<Ring>>>()
            for ((featureIndex, feature) in root.get("features").withIndex()) {
                val props = feature.get("properties")
  after: |2-
                    ?: error("GeoJSON feature #$featureIndex has neither a level1 nor a name property")
                val geometry = feature.get("geometry")
                val coords = geometry.get("coordinates")
links: []
author: agent
agent: claude-code
orphan: false
createdAt: 1784063697230
updatedAt: 1784065525879
---

(props.get("level1") ?: props.get("name")).asString() throws NPE if a feature has neither property. map.json always has level1 today, so this is latent, but a feature with a missing/null name would crash with an opaque NullPointerException. A require(name != null) carrying the feature index would fail with a useful message.

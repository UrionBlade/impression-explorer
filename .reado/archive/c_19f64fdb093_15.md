---
id: c_19f64fdb093_15
type: note
state: done
kind: task
anchor:
  file: apps/web/src/components/organisms/UsChoropleth.tsx
  scope: range
  startLine: 106
  endLine: 106
context:
  snippet: '  // Densest state derived client-side, so the default readout doesn''t depend on'
  before: |2-
      }

      const total = data.data?.total ?? 0;
  after: |2-
      // the endpoint's result ordering.
      const topState = useMemo(() => {
        let name: string | null = null;
links: []
author: agent
agent: claude-code
orphan: false
createdAt: 1784105775251
updatedAt: 1784106355139
---

topState = states[0] treats the first entry as the densest, which holds only because the endpoint sorts by count DESC. The frontend does not sort, so if that ORDER BY were ever dropped the default readout would label an arbitrary state as the densest. Either treat the ordering as part of the API contract (and say so) or derive the max client-side so the component is self-contained.

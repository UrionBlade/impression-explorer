---
id: c_19f64fdb2a9_16
type: note
state: done
kind: task
anchor:
  file: apps/web/src/components/organisms/UsChoropleth.tsx
  scope: range
  startLine: 134
  endLine: 134
context:
  snippet: '              tabIndex={0}'
  before: |2-
                  stroke="var(--canvas)"
                  strokeWidth={0.5}
                  style={{ opacity: reduced ? 1 : 0, cursor: "pointer", outline: "none" }}
  after: |2-
                  role="img"
                  aria-label={`${s.name}: ${formatNumber(counts.get(s.name) ?? 0, locale)}`}
                  onMouseEnter={() => setActive(s.name)}
links: []
author: agent
agent: claude-code
orphan: true
createdAt: 1784105775785
updatedAt: 1784106355142
---

Every state path is tabIndex=0, so keyboard users tab through ~50 stops before anything after the map. Focus updating the readout is good, but there is no non-visual equivalent. Consider a roving tabindex (one stop for the map, arrow keys between states) and/or a visually-hidden data table as the accessible counterpart to the choropleth.

<!-- reado:reply author=agent agent=claude-code at=1784106355117 -->

Done: dropped the ~50 per-path tab stops (paths are pointer-only now, svg aria-hidden) and added a visually-hidden data table (state + impressions) as the accessible equivalent, rather than a roving-tabindex.

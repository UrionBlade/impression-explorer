---
id: c_19f64f3df27_10
type: note
state: done
kind: task
anchor:
  file: apps/web/src/main.tsx
  scope: range
  startLine: 40
  endLine: 41
context:
  snippet: |2-
                  <Route path="/:lang/*" element={<LocalizedApp />} />
                  <Route path="*" element={<RedirectToDefaultLocale />} />
  before: |2-
              <BrowserRouter>
                <Routes>
                  {/* `/*` leaves room for per-language sub-routes in later metric slices. */}
  after: |2-
                </Routes>
              </BrowserRouter>
            </MotionProvider>
links: []
author: agent
agent: claude-code
orphan: false
createdAt: 1784105131815
updatedAt: 1784106355121
---

Routing is single-segment: path="/:lang" matches /it and /en but NOT a deeper path like /it/map, which falls through to the catch-all and redirects to /${detectLocale()} - dropping the original path and any query string. Fine while this is one page. When the later metric slices add routes, switch to "/:lang/*" with nested <Routes>, and consider carrying the original path through the redirect so a deep link resolves instead of bouncing to the language root.

<!-- reado:reply author=agent agent=claude-code at=1784106355108 -->

Done: route is now /:lang/* (ready for per-language sub-routes) and the redirect preserves the query string via a shared RedirectToDefaultLocale. Full deep-path preservation is deferred until real sub-routes exist (today it'd be ambiguous to tell an invalid lang segment from a path segment).

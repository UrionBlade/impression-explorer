---
id: c_19f627bb3c9_5
type: question
state: done
kind: task
anchor:
  file: apps/api/src/main/kotlin/com/cuebiq/impressions/seed/SeedRunner.kt
  scope: range
  startLine: 94
  endLine: 94
context:
  snippet: '                    if (line.isBlank()) return@forEachLine'
  before: |2-
                            }
                            return@forEachLine
                        }
  after: |2-
                        val f = line.split(',')
                        require(f.size == 4) { "Malformed row at line $lineNo: expected 4 fields, got ${f.size}" }
                        val deviceId = f[0].trim().toLongOrNull() ?: error("Bad device_id at line $lineNo: '${f[0]}'")
links: []
author: agent
agent: claude-code
orphan: false
createdAt: 1784063701961
updatedAt: 1784065525869
---

Line 1 is treated as a header and skipped unconditionally. Everywhere else the loader fails loudly on malformed input, but a header-less or column-reordered CSV would silently lose the first data row (or shift columns) with no error. Should the seed validate that line 1 equals the expected header (device_id,lat,lng,timestamp) and fail otherwise, rather than blind-skipping?
